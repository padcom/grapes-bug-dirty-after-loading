# GrapesJS marks project as modified upon loading

This example showing how the GrapesJS editor marks the project as changed upon loading it manually.
It doesn't not mark it as changed when using `autoload`

